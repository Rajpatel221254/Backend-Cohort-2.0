const app = require('./src/app') // aquire a app from app.js to start a server

app.listen(3000,()=>{
    console.log("Server is running on port 3000");
})
// starting a server and once start print message in console....